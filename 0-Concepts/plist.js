console.log("Thapa List");
/**
 * !
 * ?
 * *
 * @param
 * TODO
 */
//? Data Types (number, string, boolean, BigInt, any, unknown, )
//? Type Inference (ability of the TSC to automatically define and assign types to variables, expressions and function return values based on their usage and context in the code.) 
//? Type Annotation (explicitly defining the tyepe)  
//? Functions (function_declaration, annonymous_function, arrow_functions)
//? Optional (?) and Default parameters (=)   
//? Optional Chaining(?:,?.)   
//? Strings
/**
 * * includes(), chartAt(), indexOf()
 * * toLowerCase(), toUpperCase()
 * * slice, split(), reverse(), join(), concat()
 * * toString()
 * * length()
 *  */
//? Arrays ([1,2,3], new Array(1,2,3), Array.of(1,2,3))
/**
 * * push(), pop()
 * * shift(), unshift()
 * * sort()
 * * forEach()
 * * map(), filter(), reduce()
 */
//? Loops (for, for in, for of, forEach)  
//? Objects ({name: "ali"})
/**
 * * spread_Operator(...obj)
 * * this, call(), apply(), bind()
 * * assign()
 * * create()
 * * entries()
 * * values()
 * * is()
 * * freeze()
 * * valueOf(), toString(), toLocalString()
 * * isPrototypeOf()
 */
//? Type Alisas, Enums, Tuples (readOnly)
//? Unions (|) and Intersections (&)
//? Generics (<T, U>)
//? Function Overloading with Generics 
//? Interfaces
//? tsconfig.json
//? REST API project
//? OOP
/**
 * * state, behaviour, constructor, this, super, extends, implements
 * * Inheritance, Polymorphism, Abstraction, Encapsulation
 * * public, protected, private, readonly, getter, setter
 * * Static Properties and Methods
 * * Abstract Classes
 */
//? Union & Intersection
//? Type Narrowing
